Place the **Agustina Regular** font files here so the signature brand renders the same on every device.

Expected filenames:
- `public/fonts/Agustina-Regular.woff2` (preferred)
- `public/fonts/Agustina-Regular.woff` (fallback)

